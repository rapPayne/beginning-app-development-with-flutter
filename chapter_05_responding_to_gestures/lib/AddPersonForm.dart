import 'package:flutter/material.dart';

class AddPersonForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("I'm addPerson form"),)
    );
  }
}